apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: java-pure-apko
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /$2


spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /java-pure-apko(/|$)(.*)
            pathType: Prefix
            backend:
              service:
                name: java-pure-apko
                port:
                  number: 80