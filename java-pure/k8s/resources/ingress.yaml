apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: java-pure
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /$2


spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /java-pure(/|$)(.*)
            pathType: Prefix
            backend:
              service:
                name: java-pure
                port:
                  number: 80