# Python-Flask application for texas


## Running the application
- `pip install -r requirements.txt`
- `./boot.sh`


## Running tests
- in app directory: `python -m pytest`
- see coverage of whole project: `pytest --cov=./ tests/`