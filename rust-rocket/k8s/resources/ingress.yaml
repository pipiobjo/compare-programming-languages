apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rust-rocket
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /$2


spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /rust-rocket(/|$)(.*)
            pathType: Prefix
            backend:
              service:
                name: rust-rocket
                port:
                  number: 80